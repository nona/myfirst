# Required metadata
sonar.projectKey=myfirst
sonar.projectName=myfirst
sonar.projectVersion=0.0.1-SNAPSHOT

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8